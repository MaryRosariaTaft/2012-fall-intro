(define (square x)
  (* x x))

(define (sum-of-squares a b)
  (+ (* a a)
     (* b b)))

(define pi 3.14)
(define (areacircle r)
  (* pi (square r)))

(define (AreaRectangle a b)
  (* a b))

(define (AreaTriangle b h)
  (/ (* b h) 2))